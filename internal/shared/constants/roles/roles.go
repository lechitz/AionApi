// Package roles defines standard application role constants used for authorization checks.
package roles

const (
	// DefaultUser is the baseline role granted to regular users.
	DefaultUser = "user"
	// Admin is the elevated role with administrative privileges.
	Admin = "admin"
)
