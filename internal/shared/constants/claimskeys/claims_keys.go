// Package claimskeys contains constants for JWT claim keys.
package claimskeys

const (
	// UserID is the JWT claim key for user ID.
	UserID = "user_id"

	// Exp is the JWT claim key for expiration time.
	Exp = "exp"
)
