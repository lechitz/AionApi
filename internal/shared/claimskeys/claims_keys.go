// Package claimskeys contains constants used for JWT claims.
package claimskeys

// UserID is the key to identify the user ID in JWT claims.
const UserID = "user_id"

// Exp is the key to identify the expiration time in JWT claims.
const Exp = "exp"
