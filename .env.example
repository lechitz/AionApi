# ======================================
# Server configuration
# ======================================

# Application name (unique service identifier)
SERVER_CONTEXT=aion-api
# Port where the application will be exposed
PORT=5001

# ======================================
# Application environment
# ======================================

# Application environment: development, production, or staging
APP_ENV=development
# Logging level: debug, info, warn, error
LOG_LEVEL=debug

# ======================================
# Database configuration
# ======================================

# Database type (e.g., postgres, mysql)
DB_TYPE=postgres
# Database username
DB_USER=<your_db_user>
# Database password
DB_PASSWORD=<your_db_password>
# Database name
DB_NAME=<your_db_name>
# Database host
DB_HOST=localhost
# Database port
DB_PORT=5432
# Maximum connections in the pool
DB_MAX_CONNECTIONS=20
# SSL mode for database connection (disable, require, etc.)
DB_SSL_MODE=disable
# Path to your Go project
AION_GO_FOLDER=/path/to/your/project

# ======================================
# Secret key for JWT
# ======================================

# Secret key used for signing JWT tokens
SECRET_KEY=<your_secret_key>
# JWT token expiration time (in seconds)
JWT_EXPIRATION=3600
# JWT issuer identifier
JWT_ISSUER=AionApi

# ======================================
# Redis configuration
# ======================================

# Redis server address
REDIS_ADDR=localhost:6379
# Redis password (leave blank if not applicable)
REDIS_PASSWORD=<your_redis_password>
# Redis database (number)
REDIS_DB=0
# Maximum connection pool size
REDIS_POOL_SIZE=10

# ======================================
# Postgres configuration (Docker-specific)
# ======================================

# Postgres database username
POSTGRES_USER=<your_postgres_user>
# Postgres database password
POSTGRES_PASSWORD=<your_postgres_password>
# Postgres database name
POSTGRES_DB=<your_postgres_db>
# Timezone for the database
TZ=America/Sao_Paulo

# ======================================
# Miscellaneous settings
# ======================================

# Default timeout for HTTP requests (in seconds)
REQUEST_TIMEOUT=30
# Default timezone for the application
TIMEZONE=America/Sao_Paulo
