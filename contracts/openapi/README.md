# AionApi OpenAPI (REST) Contracts

This directory contains the OpenAPI/Swagger specifications for AionApi REST endpoints.

## 📄 Files

- `swagger.json` - OpenAPI 3.0 specification (JSON format)
- `swagger.yaml` - OpenAPI 3.0 specification (YAML format)
- `docs.go` - Embedded docs for Swagger UI

## 🔗 Endpoints

### Authentication
- `POST /auth/login` - Authenticate user
- `POST /auth/logout` - End session
- `GET /auth/session` - Get session info

### User Management
- `GET /user/all` - List all users
- `POST /user/create` - Create new user
- `GET /user/{user_id}` - Get user by ID
- `PUT /user` - Update user
- `DELETE /user` - Delete user
- `PUT /user/password` - Change password

### Admin Operations
- `PUT /admin/users/{user_id}/block` - Block user
- `PUT /admin/users/{user_id}/unblock` - Unblock user
- `PUT /admin/users/{user_id}/promote-admin` - Promote to admin
- `PUT /admin/users/{user_id}/demote-admin` - Demote from admin
- `PUT /admin/users/{user_id}/roles` - Update user roles

### Chat
- `POST /chat/text` - Send text message
- `POST /chat/audio` - Send audio message

## 🚀 Usage

### View Swagger UI (Local)

```bash
# Start API
make dev

# Open browser
open http://localhost:5001/swagger/index.html
```

### Generate Client

#### TypeScript
```bash
openapi-generator-cli generate \
  -i contracts/openapi/swagger.json \
  -g typescript-axios \
  -o clients/typescript
```

#### Python
```bash
openapi-generator-cli generate \
  -i contracts/openapi/swagger.json \
  -g python \
  -o clients/python
```

## 🔄 Regenerate Spec

```bash
# Swagger annotations in Go code → OpenAPI spec
make swag
```

## 📚 Documentation

- **Base URL:** `http://localhost:5001/aion/api/v1`
- **Authentication:** Bearer token (JWT) via cookie or header
- **Content-Type:** `application/json`

---

**Auto-generated by swag from Go annotations**  
Last updated: 2026-02-04
