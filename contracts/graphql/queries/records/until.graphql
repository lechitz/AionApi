query RecordsUntil($until: String!, $limit: Int) { recordsUntil(until: $until, limit: $limit) { id userId tagId description eventTime recordedAt durationSeconds value source timezone status createdAt updatedAt } }
