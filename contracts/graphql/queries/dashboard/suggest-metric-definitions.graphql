query SuggestMetricDefinitions($limit: Int) { suggestMetricDefinitions(limit: $limit) { metricKey name description icon valueType format precision defaultPeriod defaultAggregation defaultVisible goalDefault displayOrder metadata } }
